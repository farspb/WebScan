# Requirements for WebScan
# Core web framework and queuing
Flask>=2.0
rq>=1.9
redis>=4.0

# Database
SQLAlchemy>=1.4

# DNS, networking, parsing
dnspython>=2.0
requests>=2.28
beautifulsoup4>=4.11
python-whois>=0.7

# Optional: Nmap Python bindings (requires nmap installed on the host)
python-nmap>=0.6  # optional: only needed if you use nmap integration

# TLS / crypto
pyOpenSSL>=23.0
cryptography>=40.0

# Browser automation
playwright>=1.40.0  # after installing, run: `playwright install` to install browser binaries
selenium>=4.8
webdriver-manager>=4.0  # optional: helps auto-download browser drivers for selenium

# Docker tooling
docker>=6.0
docker-compose>=1.29.2  # optional: on newer Docker CLI prefer the 'docker compose' plugin

# Notes:
# - Some packages (cryptography, pyOpenSSL, playwright) require additional system dependencies
#   or manual steps (e.g. `playwright install`, apt packages for ssl/dev tools). See their docs.
# - If you want reproducible installs, consider pinning exact versions or using a lock file (pip-tools/poetry).
